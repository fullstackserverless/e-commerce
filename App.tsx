import React from 'react'
import { SafeAreaView, Button } from 'react-native'
import { ExampleForm } from './src/components'
const App = () => {

  return (
    <SafeAreaView style={{flex: 1}}>
      <ExampleForm />
    </SafeAreaView>
  )
}

export default App
