import React, { memo } from 'react'
import { View, ViewProps } from 'react-native'

type CheckBoxType = 'active' | 'inActive'

export interface CheckBoxProps {
    isActive: boolean;
}

const CheckBox = () => {

}
